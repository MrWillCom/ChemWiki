import { Callout, Tab, Tabs } from "nextra-theme-docs";

# Home

ChemWiki（/kemwɪkɪ/）旨在收集和整理中学化学的各类知识，将它们互相连接起来，便于检索和学习，适用于使用沪科版化学教材的同学。

<Callout emoji="👀">
  想要跟随课本进度的笔记？请查阅[上海高中课本知识整理](https://knowledge.clckblog.space/)。
</Callout>

## 规范

对于一般的中文文案排版，遵循 [sparanoid/chinese-copywriting-guidelines](https://github.com/sparanoid/chinese-copywriting-guidelines) 的方案。

## 编辑

你不需要配置开发环境，可以直接编辑 [MDX](https://mdxjs.com/) 文件，它支持所有 Markdown 语法，还添加了 JSX 支持。

本站基于 [Nextra](https://nextra.site/)，你也可以调用其附带的 JSX 组件。另外，你还可以使用 LaTeX 输入公式。

<Tabs items={["Nextra 内建组件", "LaTeX"]}>
  <Tab>
    你可以使用 `<Callout />` 强调一些内容，如：
    <Callout type="warning">
      该反应是中学化学中唯一一个同时生成烟和雾的反应。
    </Callout>
    `<Callout />` 有多种类型，请参阅：[Callout Component -
    Nextra](https://nextra.site/docs/docs-theme/built-ins/callout)。
    
    另外还有 `<Tabs />` 和 `<Tab />` ，请参阅：[Tab Component -
    Nextra](https://nextra.site/docs/docs-theme/built-ins/tabs)。
  </Tab>
  <Tab>
示例：

$\ce{Cl2}$ 与 $\rm SO_2$ $1:1$ 混合，失去漂白作用。

$$
\ce{SO2 + Cl2 + 2H2O = H2SO4 + 2HCl}
$$

```md
$\ce{Cl2}$ 与 $\rm SO_2$ $1:1$ 混合，失去漂白作用。

$$
\ce{SO2 + Cl2 + 2H2O = H2SO4 + 2HCl}
$$
```

  </Tab>
</Tabs>

## 开发

### 安装依赖

```sh
pnpm install
```

### 启动开发服务器

```sh
pnpm dev
```

### 为生产环境构建

```sh
pnpm build
```

### 在生产环境启动

```sh
pnpm start
```
